{
  "name": "@ocp/rules-engine",
  "version": "0.0.1",
  "private": true,
  "description": "Institutional rules engine for Open Career Profile — validates and transforms profiles",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/ --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ocp/core": "*"
  },
  "devDependencies": {
    "@types/node": "22.15.17",
    "vitest": "3.1.4",
    "typescript": "5.8.3"
  }
}
